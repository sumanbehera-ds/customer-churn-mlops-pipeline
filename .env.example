# MLflow
MLFLOW_TRACKING_URI=

# Model artifact paths
MODEL_PATH=models/model.ubj
JOBLIB_MODEL_PATH=models/model.pkl
PREPROCESSOR_PATH=models/preprocessor.pkl

# FastAPI / Render
PORT=10000

# DVC Google Drive remote
# Keep DVC credentials local in .dvc/config.local and .dvc/secrets/.
# For My Drive uploads, configure OAuth locally with `dvc remote modify --local`.
